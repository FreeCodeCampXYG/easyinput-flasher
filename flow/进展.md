# 进展日志(append,新的放最上面)

## 2026-09-02 · 六平台单平台重试与合并发布 · Codex
- 做了什么: 撤销 Windows-only 误改，恢复六平台 CI/Release；新增 `Retry Platform Build` 和 `Publish Existing Release` 的跨 run artifact 合并能力。
- 为什么这么做: 用户要求保持 Windows、macOS、Linux 和 ARM64 的完整推广包；慢平台只应单独取消/重跑，不能丢弃已成功的平台成果。
- 当前证据: `v0.1.22` 原 run `33642934892` 的五个平台 artifact 仍存在，仅 Linux x64 缺失；新发布步骤会强制检查六平台 artifact，防止不完整 Release。
- 验证: 四份 workflow YAML 解析、差异检查通过。
- 下一步: 推送流程配置到 main 后，手动重跑 `v0.1.22` Linux x64，使用原 run 与重试 run 合并发布；不重推 tag。

## 2026-09-02 · Windows x64 单平台发布 · Codex
- 做了什么: 请求取消卡住 Linux WebKit 安装的 `v0.1.22` Release run `33642934892`，并将 CI/Release 矩阵收敛到 Windows x64。
- 结果: run 最终为 `cancelled`，没有创建 Release；后续推送只构建 Windows x64，发布汇总只消费该 artifact，不再等待 Linux、macOS 或 Windows ARM64。
- 验证: `ci.yml` 与 `release.yml` YAML 解析、差异检查通过。
- 下一步: 以新 tag `v0.1.23` 提交该 workflow 配置并触发单 Windows x64 构建；确认 Release 包后再安装验证排序，不能重推 `v0.1.22`。

## 2026-09-02 · v0.1.22 排序修复待发布 · Codex
- 做了什么: 确认 `v0.1.21` 已在排序修复之前发布；前端适配层补充与后端一致的语义版本排序，避免旧后端或缓存快照再次按 GitHub 返回顺序展示。
- 结果: 新包会将 `v0.2.12`、`v0.2.11`、`v0.2.10`、`v0.2.9` 依次降序显示；不能覆盖或重推已发布的 `v0.1.21`。
- 验证: 前端 typecheck/build、固件包定向测试、定向 go vet、差异检查通过。
- 下一步: 使用新 annotated tag `v0.1.22` 触发打包，再在实际桌面端刷新列表复核；构建/Release/实机结果均须单独回读。

## 2026-09-02 · 固件 Release 语义版本排序 · Codex
- 做了什么: 在后端 `ListReleases` 增加 tag 语义版本降序，新增排序单测。
- 问题→解决: GitHub API 的发布时间顺序会令 `firmware-v0.2.10` 排在 `firmware-v0.2.8` 之后；现在 `vX.Y.Z` 三段数值比较，稳定版本优先同号后缀版本，无法解析时回退发布日期。
- 验证: `go test ./internal/firmware`、前端 typecheck/build、go vet、差异检查通过；全量 Go 测试仍受既有 Windows `internal/application` 测试二进制访问拒绝阻断。
- 产出(路径): `internal/firmware/github.go`、`internal/firmware/github_test.go`、`DEV_STATE.md`。
- 下一步: 在桌面端刷新 Release 列表，确认 `v0.2.10`、`v0.2.9`、`v0.2.8` 的显示顺序；不影响已选固件或烧录门禁。

## 2026-09-02 · v0.1.21 GitHub 交付计划 · Codex
- 做了什么: 核对 `main`/`origin/main` 对齐、许可证与 GitHub 模板、远端 tag、Actions 身份和 Release workflow。
- 结果: 仅三项本轮文件待提交；最新远端 tag 是 `v0.1.20`，建议使用 annotated tag `v0.1.21`。`.codegraph/` 是本地索引，明确不提交。
- 下一步: 用户逐条执行提交、显式 main refspec push、tag push 后，分别回读远端 tag 与六平台 Release workflow；不能将 tag 已推送等同于构建、Release 或实板验证成功。

## 2026-09-02 · 烧录页紧凑布局验证 · Codex
- 做了什么: 完成前端类型检查、生产构建和 `2048×1152` 本地预览截图复核。
- 结果: 右栏确认输入、主按钮、最近活动入口均在首屏；“等待检测设备”进度条已保留左右内边距，辅助信息为 `9px`，主操作字号未缩小。
- 问题→解决: 原进度面板使用 `padding: 13px 0 15px`，导致状态文字和进度轨道贴边；现限定烧录页为 `padding: 10px 12px 11px`。
- 产出(路径): `frontend/src/styles.css`、`DEV_STATE.md`。
- 下一步: 真实设备流程仍待实板验证；本轮只验证布局，不改变确认门禁或烧录状态机。

## 2026-09-02 · 烧录页首屏紧凑化 · Codex
- 做了什么: 收紧烧录页顶部、标题说明、步骤条、双栏与模块的垂直间距；“等待检测设备”进度区域补足左右 `12px` 内边距，并将地址、校验、日志等辅助字调整为 `9px`。
- 为什么这么做: 用户反馈确认控件仍需下滚才能看到，且等待检测状态条内容贴边；主操作与安全状态保持原字号，避免为了塞进首屏而损失可读性。
- 产出(路径): `frontend/src/styles.css`、`DEV_STATE.md`。
- 验证: 待执行前端 typecheck/build 和桌面截图复核。
- 下一步: 在桌面窗口确认 100% 缩放下右栏确认区域与状态条留白；不以静态布局调整替代真实设备流程验证。

## 2026-09-02 · v0.1.20 推送与发布构建 · Codex
- 做了什么: 将 `c3342fd` 推送到 `origin/main`，创建并推送 annotated tag `v0.1.20`，触发 GitHub Release workflow。
- 结果: Release workflow `33581134052` 当前 `in_progress`，会执行 Windows x64/ARM64、macOS Intel/Apple Silicon、Linux x64/ARM64 六平台编译打包。
- 问题→解决: 首次 tag 推送因 GitHub 连接超时失败，重试后已成功；未重复提交。
- 下一步: 回读 workflow 六平台结果和 Release 资产，不能将进行中状态写成发布成功。

## 2026-09-02 · 烧录确认与诊断交互提示 · Codex
- 做了什么: 右栏控制区改为滚动时保持可见；确认口令增加“一键填入”；烧录进行中/恢复阶段显示操作提醒，完成后自动隐藏；诊断页为开始诊断、固件切换和逐项标记补充状态反馈。
- 为什么这么做: 用户反馈文字贴边、确认按钮需要滚动、手工输入 MAC 口令耗时，以及诊断点击后像无响应。
- 关键边界: 一键填入不绕过后端确认；教学固件选择只更新说明，不自动烧录；诊断通过仍须真实设备观察或 HID 遥测。
- 验证: 前端 `npm run build` 通过；Go 定向包测试通过，application 测试执行被 Windows `Access is denied` 拦截；`go vet ./...`、`git diff --check` 已执行。下一步做桌面截图和新固件实板诊断回归。

## 2026-09-02 · 烧录段日志语义修正 · Codex
- 做了什么: 将每段镜像的 0% 事件改为“开始写入”，完成事件改为“写入完成（100%）”；完整明细按开始/进行中/完成显示状态。
- 为什么这么做: 用户看到同一镜像 0% 与 100% 容易误认为重复写入或日志延迟，实际是开始/完成两个真实回调节点。
- 验证: 前端 typecheck/build、Go 定向测试、go vet、差异检查通过；application 测试仍受 Windows `Access is denied` 拦截。

## 2026-09-02 · 完整烧录明细面板 · Codex
- 做了什么: 在烧录页右侧增加三段镜像明细，展示固定地址、当前段字节进度、等待/进行中/已完成状态和全部烧录事件。
- 为什么这么做: 仅显示总进度 100% 无法确认是哪一段完成，也不利于定位中途失败或重复写入。
- 验证: 前端 typecheck/build、`git diff --check` 通过。

## 2026-09-02 · 烧录页右栏间距与日志入口 · Codex
- 做了什么: 调整右侧写入控制栏的内边距、模块间距和日志预览留白；将“查看全部”接入设备与日志页面跳转。
- 为什么这么做: 截图显示右栏信息贴边、日志入口点击无效，完成状态与最近活动难以扫描。
- 验证: 前端 typecheck/build、`git diff --check` 通过；未做桌面截图回归。

## 2026-09-02 · 诊断无响应根因与报告类型修复 · Codex
- 做了什么: 定位 Maker `0x13` 状态请求是 Feature Report，而 Flasher Go HID 只能发送 Output；已在 Maker 描述符和回调改为兼容 Output，Flasher 请求格式不变。
- 结果: 旧固件不会响应当前自动诊断；需烧录包含 Maker 修复的新固件后再验证正常 HID 状态回报。
- 验证: Maker `config_status_tests`/`status_hid_protocol_tests`、Flasher Go 定向测试、go vet、前端 typecheck/build 通过；Flasher application 测试被 Windows `Access is denied` 拦截。

## 2026-09-01 · Windows ARM64 原生构建的 HID 依赖降级 · Codex
- 做了什么: 将 karalabe/hid 真实诊断读取限定在 Windows amd64 或非 Windows 架构；新增 Windows ARM64 纯 Go 降级实现，明确返回未包含 hidapi 能力。
- 问题→根因: ARM64 runner 没有可用的 Windows ARM64 hidapi/CGO 编译器，Wails 原生构建调用 x64 GCC 汇编 ARM64 cgo 文件失败。
- 怎么理解: ARM64 包恢复可编译，但该架构暂不提供自动 HID 遥测；不伪造检测结果。Windows x64/macOS 仍保留真实读取路径。
- 产出(路径): `internal/device/hid_diagnostics.go`、`internal/device/hid_diagnostics_windows_arm64.go`、`DEV_STATE.md`。
- 验证: `GOOS=windows GOARCH=arm64 CGO_ENABLED=0 go build ./...` 通过；待提交后重新跑 ARM64 原生 Actions。
- 下一步(交接棒): 提交并推送修复，单独观察 Windows ARM64 quality；若需要 ARM64 自动 HID，再引入经验证的纯 Go Windows HID 后端。

## 2026-09-01 · Windows ARM64 CI 的 CGO 测试修复 · Codex
- 做了什么: 将 CI 的 Go 单测和 vet 阶段设置 `CGO_ENABLED=0`，仅原生 Wails 构建继续启用 CGO。
- 问题→根因: Windows ARM64 runner 使用 x64 GCC 汇编 `karalabe/hid` 的 ARM64 C 文件，导致 `stp x29,x30` 等汇编错误。
- 怎么理解: 单测不需要真实 HID，关闭 CGO 使用库的纯 Go 禁用实现；这不改变最终 Windows ARM64 应用的 HID 构建路径。
- 验证: 本地非 application 包测试通过；application 测试仍受 Windows `Access is denied` 拦截；差异检查通过。
- 下一步(交接棒): 提交并推送该 workflow 修复，确认六平台 CI 全绿；再单独关注 ARM64 原生 Wails 构建是否通过。

## 2026-09-01 · 用户学习排查动态观察台 · Codex
- 做了什么: 在 S1-S8 诊断卡中增加动态输入观察台；按实体键并确认真实反馈后，点击对应编号记录观察次数，提供即时高亮动画。
- 为什么这么做: 当前没有专用诊断协议，无法自动读取按键事件；用户仍需要一个适合教学的初步排查入口。
- 怎么理解: 观察台记录的是用户观察，不是硬件自动通过；动画只反馈记录动作，不能替代 USB/BLE HID 或固件日志中的真实输入证据。
- 产出(路径): `frontend/src/App.tsx`、`frontend/src/styles.css`、`DEV_STATE.md`。
- 验证: 前端 typecheck/build、`git diff --check` 通过；未做实板输入回归。
- 下一步(交接棒): 在真实固件下按 S1-S8 验证系统输入反馈与教学文案；如未来需要自动检测，再设计专用固件诊断协议。

## 2026-09-01 · 无线、电池与真实输入反馈诊断项 · Codex
- 做了什么: 新增蓝牙 BLE、Wi-Fi、USB HID、电池剩余容量诊断项；扩充按键/旋钮/USB HID 的真实反馈测试步骤。
- 为什么这么做: 板上已集成无线和电池采样能力，但原诊断清单覆盖不足；用户还需要知道“什么反馈才算通过”。
- 怎么理解: 电池百分比只能是固件估算，不能由 GPIO4 电压直接证明；无线能力需当前固件启用；Flasher 按钮点击不是硬件输入事件。
- 产出(路径): `internal/application/app.go`、`frontend/src/App.tsx`、`DEV_STATE.md`。
- 验证: 前端 typecheck/build、Go 定向包测试、`go vet ./...`、`git diff --check` 通过；application 测试执行受 Windows `Access is denied` 拦截，未做实板回归。
- 下一步(交接棒): 如需自动判定按键/旋钮/无线/电池结果，应让目标固件提供只读诊断协议，再单独设计版本化契约。

## 2026-09-01 · 诊断测试步骤与 BOOT/运行态切换提示 · Codex
- 做了什么: 为按键、旋钮、灯光、麦克风、扬声器、USB/BLE HID 增加逐项测试步骤，并在诊断页顶部明确自动验身完成后要关机再正常开机，回到运行态才能测试外设。
- 为什么这么做: 用户反馈按 BOOT 后只能看到项目，却不知道怎样操作才可以标记通过；下载模式只适合 ROM 验身，不能代表固件功能正在运行。
- 怎么理解: 每个步骤只描述可观察的通用动作，具体快捷键、颜色、音频参数和 HID 行为继续由当前固件决定；“标记通过”仍是用户确认，不是烧录器自动检测。
- 产出(路径): `frontend/src/App.tsx`、`frontend/src/styles.css`、`DEV_STATE.md`。
- 验证: 前端 typecheck/build、`git diff --check` 通过；未做实板回归。
- 下一步(交接棒): 在不同固件和真实 EasyInput V2.0 上按步骤操作，确认用户反馈入口、正常模式 HID 和外设观察结果与提示一致。

## 2026-09-01 · 诊断页教学提示与固件切换 · Codex
- 做了什么: 诊断页增加“教学固件”选择器和每项硬件的白话“怎么用”提示，并展示所选 Release 的适配板型、Factory 用途和已有功能/变更摘要。
- 为什么这么做: 诊断页需要服务教学，不应只给出通过/失败；未来切换固件时，说明应跟随 manifest 变化而不是复制某个固件的产品文案。
- 怎么理解: 内嵌内容只讲 EasyInput V2.0 的通用板级事实与安全边界；S1-S8 业务动作、LED 语义、音频参数和协议仍以当前固件说明/实板验证为准，降低侵权和误导风险。
- 产出(路径): `frontend/src/App.tsx`、`frontend/src/styles.css`、`DEV_STATE.md`。
- 验证: 前端 typecheck/build、Go 定向测试、`go vet ./...`、`git diff --check` 通过；未做实板教学回归。
- 下一步(交接棒): 在不同官方/社区 Release 间切换，确认教学摘要不会改变烧录选择；后续可为 manifest 增加结构化 usage/tips 字段，但需先定义发布合同和来源授权边界。

## 2026-09-01 · v0.1.14 推送与 Actions · Codex
- 做了什么: 提交 `0e8e578`，推送 `main`，创建并推送 annotated tag `v0.1.14`，触发 Release、CI、CodeQL。
- 验证: 远端接受 main 和 tag；run `33510516903`（Release）、`33510496423`（CI）、`33510496456`（CodeQL）已在运行。
- 下一步(交接棒): 回读六平台 Actions 结果；成功后下载 v0.1.14 macOS 归档，在 Intel/Apple Silicon 真机验证设备流程。

## 2026-09-01 · 诊断页专业化与检测误报收紧 · Codex
- 做了什么: 依据 EasyInput V2.0 板级合同重排硬件诊断页，分为自动验身、用户操作、运行态观察和待固件验证；新增推荐操作顺序、设备模式提示和结果汇总。修复浅色主题下诊断文字近乎不可读的问题。
- 为什么这么做: 原页面把不同证据等级混成同一网格，且浅色主题漏了诊断卡文字覆盖；Windows 还会把 PnP 未命中的全部 COM 口误列为 EasyInput 下载设备。
- 怎么理解: 正常 HID 只证明设备在线，BOOT 后的下载端口才允许 ROM 验身；PSRAM、电源、音频和外设功能仍保留“运行态/固件验证”边界。
- 产出(路径): `frontend/src/App.tsx`、`frontend/src/styles.css`、`internal/device/ports_windows.go`、`README.md`、`DEV_STATE.md`。
- 问题→解决: 误报来源不是 ESP32 验身失败，而是 PnP 成功后无条件追加串口列表；现仅在 PnP 查询失败时保留候选，避免把无关 COM 口伪装成目标板。
- 验证: 前端 typecheck/build 通过；Go 定向包编译通过，application 测试执行仍受 Windows `Access is denied` 拦截；未做实板验证。
- 下一步(交接棒): 在真实 Windows + EasyInput V2.0 上确认正常 HID、短按 BOOT、下载端口、错误 COM 口过滤和诊断卡对比度；收集火绒拦截名称与哈希后再决定是否补充签名/误报申诉流程。

## 2026-09-01 · macOS 设备枚举与恢复检测 · Codex
- 做了什么: 新增 macOS 专用串口/HID 扫描与恢复检测，串口可进入下载候选，USB/Bluetooth 正常模式可被识别；补充 README 的 macOS 使用说明。
- 为什么这么做: 原实现只打包 macOS，非 Windows 端口被标为 `serial-candidate` 且 HID 恢复固定失败，用户无法走完整烧录流程。
- 怎么理解: 端口仍需 ROM 读取 ESP32-S3/MAC 最终验身；系统枚举只证明候选设备存在，不替代实板功能验证。
- 产出(路径): `internal/device/ports_darwin.go`、`internal/device/hid_darwin.go`、`internal/device/ports.go`、`internal/device/hid_other.go`、`README.md`、`DEV_STATE.md`。
- 验证: Windows 定向 Go 测试、`go vet ./...`、macOS amd64/arm64 `go test -c`、`git diff --check` 通过；未在真实 Mac/实板运行。
- 下一步(交接棒): 在 Intel Mac 与 Apple Silicon Mac 上分别连接 EasyInput，验证正常模式识别、短按 BOOT 后端口选择、写入和关机重启后的 HID 恢复。

## 2026-09-01 · 产品定位与更新日志展示 · Codex
- 做了什么: 统一烧录页、关于页和 README 的产品定位为“EasyInput 固件烧录与硬件诊断工具”；更新页补齐 v0.1.10/v0.1.11/v0.1.12 功能日志与后续构建规划。
- 验证: 前端 typecheck、生产构建、差异检查通过；本轮未提交或发布。
- 下一步(交接棒): 用户确认文案后，可随下一次功能提交一起推送并重新打包。

## 2026-09-01 · CY Factory Release 兼容与项目链接 · Codex
- 做了什么: 关于页加入 Flasher GitHub 与 CY 老师 v0.4.53 Release 链接；GitHub Release 审计/列表/下载支持仅含 `factory*.bin` 的远程 Factory 项，并保留独立恢复口令和 `0x0` 固定地址。
- 为什么这么做: 让用户能直接找到项目与官方固件，同时兼容 CY 当前真实发布形式，不要求其立即改成三段 manifest 才能做恢复出厂。
- 怎么理解: Factory 只是一条破坏性恢复路径；普通三段升级合同不放宽。远程 Factory 来源的信任仍通过发现页，写入还需“确认恢复出厂 <MAC>”。
- 验证: Go vet、application 测试编译、配置/固件/烧录器测试、前端 typecheck/build、差异检查通过；未实板验证。
- 下一步(交接棒): 在实板上验证远程 v0.4.53 Factory 下载、0x0 写入、NVS/蓝牙清除和重启恢复。

## 2026-09-01 · 烧录详情与页内日志 · Codex
- 做了什么: 将 `espflasher` 真实回调映射为当前镜像、地址、当前段字节数和总进度；烧录页新增详情卡，最近活动扩展为六条并保留镜像开始/完成记录。
- 为什么这么做: 对齐参考 GUI 的地址级进度和可排查日志，避免用户必须切换页面才能确认正在写哪一段。
- 怎么理解: 地址和偏移仍来自 manifest；界面显示的是设备写入回调证据，不把构建、写入和功能验证混为一谈。
- 验证: `go vet ./...`、application 测试编译、配置/固件/烧录器测试、前端 typecheck/build、差异检查通过；未实板验证。
- 下一步(交接棒): 实板烧录时确认三段镜像的地址、进度回调与 Hash 日志和串口实际输出一致。

## 2026-09-01 · Factory 独立恢复入口 · Codex
- 做了什么: 固件库增加本地 Factory `.bin` 导入；独立标记、固定 `0x0` 写入，风险提示清除 NVS/蓝牙绑定，要求“确认恢复出厂 <MAC>”口令。
- 为什么这么做: 为首次安装和故障恢复提供明确入口，避免用户把破坏性整片镜像误当普通升级。
- 验证: `go vet ./...`、application 测试编译、配置/固件/烧录器测试、前端 typecheck/build、差异检查通过；未实板验证。
- 下一步(交接棒): 在真实 EasyInput V2.0 上验证 Factory 写入、关机重启和配置清除结果。

## 2026-09-01 · 发布包 Windows 安全提示 · Codex
- 做了什么: 在随 Windows ZIP 一同打包的根目录 `README.md` 增加“Windows 安全提示与误报”说明，覆盖未签名 SmartScreen 提示、Release SHA-256 校验、Defender 隔离后的最小化允许条件和安全反馈字段。
- 为什么这么做: Go/Wails 本地编译与便携式烧录器可能触发低信誉或启发式拦截；下载者需要先验证来源与完整性，再决定是否允许运行，而不是关闭防护。
- 怎么理解: SmartScreen 警告、Defender 隔离和已验证恶意软件是不同状态；当前未签名包只能依靠官方 Release、同名哈希与可追溯的误报反馈建立安全边界。
- 产出(路径): `README.md`（`scripts/package-windows.ps1` 已自动复制该文件进入 Windows ZIP）。
- 验证: 已静态核对 Windows 打包脚本会复制 `README.md`；文档链接和 PowerShell 校验命令待随下一次 Release 包人工复核。
- 下一步(交接棒): 生成下一版 Windows ZIP 后，下载该 ZIP，复核 README 在包内、SHA-256 命令和 GitHub 链接均可用；代码签名证书取得后再单独设计 CI 签名与时间戳流程。

## 2026-09-01 · 本地 ZIP 导入与实时写入进度 · Codex
- 做了什么: 增加本地标准 ZIP 导入与 manifest/SHA-256 校验，导入后加入固件库；普通 Release 与本地包共用固定三段镜像合同。烧录器接入 `espflasher` 进度回调，实时更新写入百分比和字节数。
- 为什么这么做: 用户需要在无 GitHub Release 时加载本地固件并保留可排查证据，同时希望看到真实烧录进展；编译功能先不纳入本轮验证。
- 怎么理解: 本地导入只接受安全 ZIP 根目录文件与已声明三段镜像，不接受任意路径/偏移；本地包来源由用户承担，实板功能仍需单独验证。
- 产出(路径): `internal/application/app.go`、`internal/flasher/flasher.go`、`frontend/src/{App.tsx,bridge.ts}`。
- 验证: `go vet ./...`、application 测试编译、前端 typecheck/build、差异检查通过；application 测试执行仍受 Windows `Access is denied` 拦截；未实板烧录。
- 下一步(交接棒): 后续可增加本地工程 `idf.py build` 调度和 Factory 恢复入口，并补充本地导入的 UI 风险二次确认与实板回归。

## 2026-09-01 · 社区 factory 镜像提示与内嵌说明 · Codex
- 做了什么: 发现页审计新增整片 `factory.bin` 识别；关于与帮助页内嵌下载模式、固件选择、验身确认、恢复和社区补齐说明；发布规范与社区指南同步说明 factory 镜像不能直接拆分。
- 为什么这么做: `CY-CHENYUE/easy-input-maker` 当前公开 Release 只有整片 factory 镜像，缺少 manifest 与三段标准资产；用户需要知道为什么“有 bin”仍不可直接烧录，也需要在应用内看到操作文档。
- 怎么理解: Flasher 不自动猜测偏移或拆分整片镜像，避免覆盖配置分区；补齐动作应由固件仓库 Actions 生成标准 Release，桌面端只消费受信 manifest。
- 产出(路径): `internal/firmware/github.go`、`frontend/src/App.tsx`、`frontend/src/styles.css`、`docs/FIRMWARE_PUBLISHING.md`、`docs/COMMUNITY_FIRMWARE_GUIDE.md`。
- 验证: `go test ./internal/firmware ./internal/config ./internal/flasher`、`go vet ./...`、前端 typecheck/build、`git diff --check` 通过。
- 下一步(交接棒): 若要让该仓库可被 Flasher 消费，应在 `CY-CHENYUE/easy-input-maker` 增加并运行标准固件发布 workflow，再用发现页重新审计 Release。

## 2026-08-31 · 首次对话设计复盘 · Codex
- 做了什么: 将复盘改为“复刻指南 + 设计复盘”，开头新增七步最小闭环、目录边界、合同、验收顺序和检查表；文末保留 GitHub 共创邀请和硬件应用 Hub 定位。
- 为什么这么做: 让新读者不必先理解完整历史，也能按明确顺序复刻安全烧录器并知道何时必须转向实机验证。
- 怎么理解: Hub 开放发现和共创，但写入仍由 Release manifest、来源信任、写前再次验身/MAC 一致性、审核和实机证据控制，不能被任意固件绕过；历史对话和设计原因退到教程之后作为解释材料。
- 产出(路径): `docs/首次对话设计复盘.md`、`docs/README.md`、`DEV_STATE.md`。
- 问题 → 怎么解决: 历史会话含大量系统指令和工具日志，不适合复制为文章；改为筛选关键用户提示/公开答复并保留会话索引。
- 下一步(交接棒): 在 GitHub Issue 收集首批硬件应用、兼容性和实机验证贡献；同时按文档第 8 节补齐官方 main 下载、写入、断电重启和 HID 恢复证据，再逐步开放精确社区 Release 审批。

## 2026-08-31 · 开源共建、Issue 分流与审核门禁 · Codex
- 做了什么: 重写 README 使用指南并加入界面截图；新增贡献指南、社区固件来源/问题 Issue 表单、CODEOWNERS、路径自动标签、CodeQL、可选 Codex 审核和维护者标签触发的原生自动合并工作流。
- 为什么这么做: 原仓库只有基础模板和默认标签，Build 模板引用不存在标签，main 无保护；社区贡献难以分流，也无法把 AI 建议与真正的合并授权区分。
- 怎么理解: DBX 的“类型 + 模块 + 平台 + 优先级”模型缩小到烧录器实际领域；合并始终要求人工批准和 CI，AI 只能补充审查意见。
- 产出(路径): `README.md`、`CONTRIBUTING.md`、`docs/images/firmware-selection.png`、`.github/{ISSUE_TEMPLATE,CODEOWNERS,labeler.yml,workflows/}`、`DEV_STATE.md`。
- 问题 → 怎么解决: 本机未安装 YAML lint 工具；已逐项审阅 workflow，并由远端 Actions 首次运行作为最终语法/权限验证。application 测试仍被本机 Windows 执行策略拦截。
- 问题 → 怎么解决: 首次 CodeQL 失败确认 Go 不支持 `build-mode: none`；工作流改为 Go autobuild、JS buildless 并升级 CodeQL v4，待推送复验。
- 下一步(交接棒): 推送配置后创建远端分类标签并启用 main 分支保护、原生 auto-merge；等待 CI/CodeQL 首次结果，再配置 `OPENAI_API_KEY` 或 GitHub Copilot 审查能力。

## 2026-08-31 · 固件摘要卡片跟随浅色主题 · Codex
- 做了什么: 为固件摘要卡片补齐浅色背景、边框、元数据和完整性提示颜色。
- 为什么这么做: 原 `.firmware-summary` 使用硬编码深色背景，浅色皮肤切换后出现截图中的深色孤岛和文字对比不协调。
- 怎么理解: 只补主题覆盖，保留深色主题原有视觉；不改变固件数据和烧录逻辑。
- 产出(路径): `frontend/src/styles.css`、`DEV_STATE.md`。
- 验证: 前端 typecheck、生产构建、差异检查通过；尚未推送。
- 下一步(交接棒): 用户确认浅色/深色切换效果后，再提交并打包。

## 2026-08-31 · 完成状态保留实际固件版本 · Codex
- 做了什么: 让恢复、完成、取消和失败状态继承本次烧录的 `firmwareId` 与设备指针。
- 为什么这么做: `CheckRecovery` 清空版本后，前端会按列表第一项回退，导致用户选择社区/其他分支后完成页误显示 main。
- 怎么理解: 只修正状态绑定，不改变固件下载、信任门禁或选择逻辑。
- 产出(路径): `internal/application/app.go`、`DEV_STATE.md`。
- 验证: Go config/firmware/flasher 测试、`go vet ./...`、前端 typecheck、差异检查通过；尚未推送。
- 下一步(交接棒): 提交并发布包含该修复的新 flasher 包，在选择非 main 固件的实机流程中回归完成页。

## 2026-08-31 · 烧录状态显示目标版本 · Codex
- 做了什么: 后端烧录各阶段状态保存 `firmwareId`，并在下载、写入、失败和恢复文案中显示选中的 manifest tag。
- 为什么这么做: 用户在实际烧录时只能看到“烧录中”，无法确认正在写入哪个固件版本。
- 怎么理解: 版本来源以 manifest tag 为准，Release 自定义标题只用于列表展示；底层选择和信任门禁保持不变。
- 产出(路径): `internal/application/app.go`、`frontend/src/bridge.ts`、`DEV_STATE.md`。
- 验证: Go config/firmware/flasher 测试、`go vet ./...`、前端 typecheck/build、差异检查通过；尚未推送。
- 下一步(交接棒): 用户确认版本提示后，再提交并创建新 flasher tag 触发打包。

## 2026-08-31 · 固件下拉标题去重 · Codex
- 做了什么: 在 `frontend/src/bridge.ts` 对 Release 标题末尾重复 tag 做显示归一化，选择框和固件库标题不再重复展示版本号。
- 为什么这么做: GitHub Release 名称 `EasyInput Firmware firmware-v0.2.3-main` 已包含 tag，原 UI 又拼接一次 tag，造成截图中的重复文本。
- 怎么理解: 只调整展示标题，烧录使用的 `repository@tag`、后端来源门禁和版本选择契约不变。
- 产出(路径): `frontend/src/bridge.ts`、`DEV_STATE.md`。
- 验证: `npm --prefix frontend run typecheck`、`npm --prefix frontend run build`、`git diff --check` 通过；尚未推送。
- 下一步(交接棒): 用户确认界面显示后，如需交付再创建新 flasher tag 并触发打包。

## 2026-08-31 · main 基线与社区固件安全边界 · Codex
- 做了什么: 后端写入前增加受信 Release allow-list，拒绝未知/未审核 `repository@tag`；manifest 固定三段文件名与 `0x0/0x8000/0x10000` 偏移；新增发布规范和回归测试。
- 为什么这么做: 前端 `trusted` 可被 Wails 调用绕过，任意 manifest 偏移也会扩大写入范围；main 分支本身没有发布工作流，不能直接被烧录器安全消费。
- 怎么理解: “还原 main”应由 Maker `main` CI 生成带 manifest 的 `firmware-v-main-*` Release；社区功能先复用同一合同，不把编译器塞进桌面端。
- 产出(路径): `internal/application/app.go`、`internal/application/app_test.go`、`internal/firmware/manifest.go`、`internal/firmware/manifest_test.go`、`docs/FIRMWARE_PUBLISHING.md`、`README.md`、`DEV_STATE.md`。
- 问题 → 怎么解决: 新 application 测试在本机 Windows 执行阶段被 `application.test.exe: Access is denied` 拦截；已确认 `go test -c` 可编译，待 CI/允许执行测试环境重跑。
- 下一步(交接棒): 在 `easy-input-maker/main` 合入发布 workflow/manifest 脚本并创建绑定 main SHA 的官方 Release，再用新烧录器验证下载、写入和 HID 恢复。

## 2026-08-31 · 纯 Go 烧录器切换与提交准备 · Codex
- 做了什么: 引入固定 `espflasher v0.8.1`，以进程内 ESP32-S3 ROM/stub 会话替代外部 esptool；删除 Python/PyInstaller/helper 打包与 GPL 副本，更新 Release 脚本、许可证说明和架构文档。
- 为什么这么做: 用户明确允许纯 Go 依赖准入；原外部 helper 增加体积、发布复杂度和 S3 stub 失败面。
- 产出(路径): `internal/flasher/`、`go.mod`、`.github/workflows/release.yml`、`scripts/package-*`、`THIRD_PARTY_NOTICES.md` 与文档。
- 验证: `go test ./...`、`go vet ./...`、`go mod verify`、前端 typecheck/build、Wails Windows x64 原生构建通过；真实写入/HID 恢复尚待实板。
- 下一步 / 交接棒: 提交并推送 main 后等待 CI；用户按短按 BOOT → 刷新端口 → 验身 → 确认写入 → 关机再开机完成实板回归。

## 2026-08-31 · S3 验身、Release 自动加载与代理修复 · Codex
- 做了什么: 将 ESP32-S3 验身从失败的 `chip-id` 改为 ROM 兼容 `read-mac`/`flash-id`，写入采用 `--no-stub`；烧录页启动自动加载受信 Release，默认/旧设置指向 `127.0.0.1:1080`，错误进入日志并反馈到界面。
- 为什么这么做: 实板只读复核已证明 COM 下载端口可连通，exit status 2 来自不适用的 S3 子命令与 stub 包装边界；GitHub `firmware-v0.2.1` 已含 manifest，空下拉框是代理继承和未自动加载共同造成。
- 产出(路径): `internal/{flasher,application,config}/`、`frontend/src/App.tsx`、`DEV_STATE.md`、本日志与决策/踩坑记录。
- 验证: `go test ./...`、`go vet ./...`、`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`、`git diff --check` 已通过；实板已只读确认 ESP32-S3，尚未用新版程序进行真实写入/恢复回归。
- 下一步 / 交接棒: 由用户短按并松开 BOOT 后用新版包刷新下载端口，验证 MAC 尾号、Release 自动出现、确认写入和关机再开机后的 HID 恢复；纯 Go `espflasher` 替换需先完成依赖准入。

> 每个 Agent 处理完一棒,在**最上面追加一条**。**顶部那条 = 当前交接棒**,下一棒读它接上。
> 这是「可复制、可回看」的接力记录:复制一条喂别的 Agent,或从上往下扫看全历史。
> 决策 → `decisions.md`,坑 → `踩坑记录.md`,内容产物 → `docs/`·`scripts/`(这里只给路径,不重抄)。

## 2026-08-30 · 明亮主题与双通道硬件检测 · Codex
- 做了什么: 将工作台主题改为明亮浅色，并让 Windows 扫描同时识别正常 HID 与下载模式串口。
- 为什么这么做: 用户反馈深色界面过暗，且正常固件仅枚举 HID 时原实现误报未检测到硬件。
- 产出(路径): `frontend/src/styles.css`、`internal/device/ports_windows.go`、`internal/application/app.go`。
- 验证: 前端 typecheck/build、Go test/vet、`git diff --check` 通过；真实设备检测待补。
- 下一步(交接棒): 在 Windows 设备上重新下载/构建后确认正常 HID 显示，进入 BOOT 后确认串口验身和烧录门禁。

## 2026-08-30 · BOOT 引导状态机修正 · Codex
- 做了什么: 参考本机 EasyInput Studio/应用层运行方式，修正烧录页顺序为正常 HID 确认、BOOT 引导、刷新下载端口、芯片/MAC 验身、人工确认和写入。
- 为什么这么做: 正常固件是 HID，不应因没有 CDC 串口被判为故障；BOOT 是用户明确操作，不能由应用假设已经完成。
- 产出(路径): `frontend/src/App.tsx`、`internal/device/ports_windows.go`、`docs/TROUBLESHOOTING.md`。
- 验证: 前端 typecheck/build、Go test/vet、`git diff --check` 通过；真实设备端口重枚举待实机验证。
- 下一步(交接棒): 在 Windows 实机确认正常 HID 到下载串口的切换，再决定是否推送和发布下一版。

## 2026-08-30 · 主题切换与正常模式设备匹配 · Codex
- 做了什么: 恢复深色默认主题并新增持久化的浅色切换；Windows 扫描加入 `EasyInput AI` BLE 正常模式匹配。
- 为什么这么做: 截图显示浅色只覆盖侧栏导致视觉割裂；本机实际设备枚举为 Bluetooth LE，不符合旧的 USB VID/PID-only 假设。
- 产出(路径): `frontend/src/{App.tsx,styles.css}`、`internal/device/ports_windows.go`。
- 验证: 本机 PnP 实际返回 EasyInput AI；Go test/vet、前端 typecheck/build、差异检查通过；提交 `49e7ead` 已推送 main。
- 下一步(交接棒): 用户切换主题并在正常模式点击重新检测；短按 BOOT 后确认下载端口和芯片/MAC 验身。

## 2026-08-31 · 正常模式与芯片验身证据分层 · Codex
- 做了什么: 正常 HID/BLE 阶段不再提前显示 ESP32-S3，右侧 BOOT 提示不再被渲染为错误。
- 为什么这么做: 设备在线不能证明芯片型号；只有下载模式下的 esptool 读取才能确认芯片和 MAC。
- 产出(路径): `internal/device/ports_windows.go`、`frontend/src/{App.tsx,bridge.ts}`。
- 验证: Go test/vet、前端 typecheck/build、差异检查通过；提交 `c7a0009` 已推送 main。
- 下一步(交接棒): 短按 BOOT 后点击刷新下载端口；出现 COM 端口才读取芯片/MAC。

## 2026-08-30 · 六平台打包与许可证边界 · Codex
- 做了什么: 将 CI/Release 扩展到六个原生 runner，补 Unix 打包脚本、PolyForm Noncommercial 自有许可证和第三方 notices。
- 为什么这么做: 多平台资产不能用交叉编译代替原生打包，且 GPL esptool 与上游固件必须保持独立许可边界。
- 产出(路径): `.github/workflows/{ci,release}.yml`、`scripts/package-unix.sh`、`LICENSE`、`THIRD_PARTY_NOTICES.md`。
- 问题 → 怎么解决: 本机没有 Git Bash，Unix 脚本留给 macOS/Linux runner 验证，不报告本机通过。
- 下一步(交接棒): 先按 Git 治理审查并创建远端，再由 CI 回读六平台真实结果。

## 2026-08-30 · Wails 烧录器首版实现中 · Codex
- 做了什么: 创建独立项目骨架，完成 Go 侧固件 manifest、哈希、GitHub 下载、设备验身和烧录状态机；前端专项正在实现。
- 为什么这么做: 固件构建与设备写入需分离，先用后端合同阻止前端或不可信 Release 绕过验证。
- 怎么理解: 当前还没有带 manifest 的 Maker Release，也未进行真实设备写入；许可证未确认前不发布。
- 产出(路径): `internal/`、`docs/`、`DESIGN.md`、`flow/plan.md`。
- 问题 → 怎么解决: 本机原先缺 Go，已安装 Go 1.27；Go 内部包测试通过。
- 下一步(交接棒): 合并前端后完成 CI、Wails Windows 构建和 Maker Release 清单工作流。

<!-- 模板(复制到最上面填):

## YYYY-MM-DD · <任务 / 这一棒> · <谁:Claude / Codex>
- 做了什么:
- 为什么这么做:
- 怎么理解:(关键假设 / 边界 / 哪些是下一棒的事)
- 产出(路径):<docs/specs/xxx.md 方案 · scripts/yyy.py 代码 …>  ← 下一棒对照 / 接着用
- 问题 → 怎么解决:(没有可省)
- 下一步(交接棒):<接手方第一个具体动作,如"对照 docs/specs/xxx.md 实现">
-->
